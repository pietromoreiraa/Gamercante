@model Gamer.Models.Game
@{
    ViewBag.Title = "Game - " + Model.Nome;
}
<h2>@Model.Nome</h2>
<p>
    @{ 
        string fileName = Model.Img;
        
        string path = System.Configuration.ConfigurationManager.AppSettings["PathWeb"] + "\\Games\\" + fileName;
    }
    <img alt="@Model.Nome"
         src="@path" />
</p>
<div id="album-details">
    <p>
        <em>Genre:</em>
        @Model.Categoria
    </p>
    <p>
        <em>Price:</em>
        @String.Format("{0:F}",Model.Preco)
    </p>
    <p class="button">
        @Html.ActionLink("Add tocart", "AddToCart","ShoppingCart", new { id = Model.GameID }, "")
    </p>
</div>